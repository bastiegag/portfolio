// import React from 'react';
// import { useThemeOptions } from 'hooks';

// export const useAnimation = (tl: React.ReactNode) => {
// 	const { themeOptions, setThemeOptions } = useThemeOptions();

// 	if (themeOptions.animate && tl) {
// 		tl.play();
// 	} else {
// 		tl.pause();
// 	}
// };
